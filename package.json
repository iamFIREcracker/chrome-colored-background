{
  "name": "colored-background",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome (MV3) extension: tmux-style page theming + tab navigation.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "test:setup": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
